# Патч-ноут — そうです (Souda)

## 0.7 (beta)
- Веду этот патч-ноут со списком изменений.
- Поправлена вёрстка: короткие подписи (чипсы, кнопки тем, оценки, метки) больше не переносятся; узкие ряды фильтров скроллятся по горизонтали.
- JLPT-пробник перенесён с главной во вкладку «Тест».
- В истории тестов указывается уровень JLPT (напр. «JLPT N5»).
- Для каждой грамматики Genki добавлено развёрнутое описание: открывается по тапу в списке и показывается на обороте карточки в «Учить».

## 0.6 (beta)
- Убраны темы «5 невест» и «Steins;Gate».
- Yuru Camp: 4 варианта фона (переключатель в настройках).
- Yuru Camp: чиби-картинка в углу (одна случайная из трёх; файлы chibi1–3.jpg в assets/themes/yuru/).
- Yuru Camp: добавлен слот «Фон 5» (файл assets/themes/yuru/yuru5.jpg).

## 0.5 (beta)
- Аниме-темы с фонами, палитрами и своими звуками (WebAudio).

## 0.4 (beta)
- Библиотеки материалов (Genki 1/2, Полезные материалы, Моя библиотека) + выбор активных.
- JLPT-пробник N5/N4/N3 (изолированный модуль, свой банк вопросов).
- Тесты: кандзи, число вопросов, история. Словарь/грамматика/кандзи: фильтры, свайпы, избранное.
- Полезные материалы, своя библиотека, статистика за 7 дней, темы и звук, и пр.

## 0.8 (beta) — в работе (4 захода)
Все запрошенные пункты, сгруппированы по областям. ✅ — сделано в этом заходе.

### Заход 1 — Исправления и корректировки
- ✅ Бэкап перед правками (папка backups/), версия → 0.8 (app + android res).
- ✅ Приветствие (おはようございます и пр.) в одну строку, шрифт ужат.
- ✅ Календарь компактнее, главная меньше требует прокрутки.
- ✅ Лаги прокрутки грамматики: делегирование событий, убраны тяжёлые слои.
- ✅ Фуригана только над кандзи (не над каной).
- ✅ В тесте убран комментарий «JLPT не из базы Genki».
- ✅ У звёздочки избранного убрана подпись (просто ★).
- ✅ Отметка «знаю»: убрана боковая надпись, выделение ярче.
- ✅ Кандзи: долгое нажатие отмечает «знаю», повторное — снимает.
- ✅ Количество новых в день: степперы (−/+) вместо ползунков (чтобы не задеть).
- ✅ Короткий коммент грамматики опционален (настройка: авто/вкл/выкл; телефон по умолч. выкл, планшет вкл).
- ✅ Учить: дневной лимит новых не сбрасывается при переключении колоды.

### Заход 2 — Новые фишки ✅
- ✅ Свои фоны: загрузка изображения в настройках, применяется к текущей теме; работает на всех темах.
- ✅ Перенос прогресса между устройствами и бэкап: экспорт/импорт файла + перенос кодом (сохраняет прогресс при переустановке APK).
- ✅ Импорт/экспорт библиотек (своя библиотека) файлом.
- ✅ Тест: первая строка «Вариант теста» (Слова/Кандзи/Грамматика/JLPT/Текст(WIP, серый)/Аудирование(WIP, серый)); подпись «Новый тест» убрана.
- ✅ Тест: лимит времени; статус (Все/Новое/Учу/Знаю); несколько уроков сразу; деление по библиотекам (мультивыбор); вариант «Недавно изученные» (последние 50 отмеченных «знаю»).
- ✅ История тестов: кнопка «История» снизу; затраченное время; открытие теста с разбором (верно/неверно по каждому вопросу); во время прохождения JLPT и история скрыты.
- ✅ Диапазон уроков перенесён в раздел «Материалы»: отдельный для каждой библиотеки (с заделом на новые учебники).

### Заход 3 — Темы и оформление ✅
- ✅ Чиби-спрайт из Yuru Camp убран.
- ✅ Стандартные фоны Yuru Camp: «Без фона», «Фон 2», «Фон 4» (остальные убраны из выбора).
- ✅ Тема «Синкай» переименована в «空», фон сделан виднее (меньше затемнение).
- ✅ При теме Yuru Camp иконка слева сверху меняется на chibi1.jpg (если файл есть; иначе обычная иконка).
- ✅ Звуки тем оставлены как есть (по согласованию).
- ℹ️ Для иконки Yuru Camp положи файл assets/themes/yuru/chibi1.jpg.

### Заход 4 — Публикация (GitHub / Boosty)
- Папка к публикации на GitHub (.gitignore, LICENSE, структура), красивый README-страница со скриншотами, упоминание Claude, Road Map.
- Версия в метаданных res (versionName/Code) — ✅ выставлено 0.8.
- Материалы для GitHub и Boosty — в отдельные папки; папка For Dev (вне git) с инструкцией по сборке APK.
- Где лежат библиотеки и как редактировать их вручную (для разработчика, без UI).
- Латиницей имя приложения — «Sodesu».

## 0.85 (beta)
- Выбор фона показывается только в темах, где фон есть (Yuru Camp).
- Фон Yuru Camp: «Фон 1» = Yurucamp BG.jpg, «Фон 2» = Yurucamp BG2.jpg (+ «Без фона»).
- Палитра темы Yuru Camp перекрашена под арт (уютная зимняя), иконка темы — арт Рин.
- Кандзи больше не выделяются как текст при долгом нажатии.
- Долгое нажатие на кандзи стало надёжным (порог движения, чистка таймера).
- Код синхронизации сразу копируется в буфер обмена (не показывается текстом).
- Свайп/кнопка «назад» возвращает на предыдущий экран, а не выходит из приложения (история навигации).
- В «Учить» добавлена фуригана (тумблер ふ в панели колод; читается только над кандзи).
- Фон-градиент вынесен в фиксированный слой — не прокручивается со страницей.
- Добавлена опция «вписать/обрезать фон под окно» (Настройки → Свой фон).
- После показа перевода в «Учить» свайпы работают: → знаю, ← дальше.
- Версия 0.85 (app + android res + strings.xml).

## 0.9 (beta)
- Описание кандзи открывается нижним листом (внизу экрана) на всех экранах.
- В карточке кандзи — кнопка «🔍 В словарь»: переходит в словарь и подставляет кандзи в поиск.
- Фуригана в «Учить» убрана из панели колод и вынесена тумблером в Настройки → «Настройки дня».
- Раздел «Настройки дня»: лимиты новых (кандзи/слова/грамматика) + тумблер фуриганы в одном месте.
- Версия 0.9 (app + android res + strings.xml).

## Подготовка релиза 0.9
- В android/app/build.gradle добавлена подпись релиза через key.properties (если файл есть — release подписывается автоматически).
- Релиз-ноуты: For Dev/github/release_notes.md. Шаблон ключа: For Dev/key.properties.example.
- README обновлён до 0.9.

## 0.9 — доп. правки
- Код переноса прогресса копируется в буфер с уведомлением; кнопка «Копировать код».
- При первом запуске — подсказки по управлению (онбординг).
- «Настройки дня» → «Настройка деки»; туда же выбор материалов деки и лимиты + фуригана.
- «Трудно» теперь повторяется только на следующий день (в этот день не показывается).
- Все системные уведомления — короткие тосты на экране (не пуш).
- Убраны оценки «Повторить/Снова» и «Хорошо».
- Добавлена 🔥 «Зазубрить»: кнопка на карточке + круглая кнопка-огонёк справа от приветствия (видна, если есть слова); отдельная частая тренировка этих слов (с эффектом горения).
- Добавлен «Архив»: отложить слово из «Учить» (не попадает в «Знаю»).
- Кнопка «Поддержка» (репорт на GitHub) и «Поддержать проект» (Boosty: boosty.to/m0thman/donate).
- Библиотеки: деление по занятиям ИЛИ по темам; для занятий — диапазон, для тем — выбор тем.
- Переработано добавление в свои библиотеки: раздел «Библиотеки» (Добавить/Экспорт .csv/Импорт .csv), редактор с выбором библиотеки/деления/группы и добавлением слов/кандзи/грамматики (Текст/Аудио — WIP). «Моя библиотека» по умолчанию убрана.
- Синхронизация: убраны экспорт/импорт файлом (только код).
- Словарь: снято ограничение на 200 показываемых слов.
- Road Map: добавлены «Переезд на C++ (Bedrock ed.)», «Релиз в RuStore», «Версия под iOS».
- Кнопка «Знаю» без «— отсеять». Подсказка в деке: «в деку (трудно)» → «в деку».

## 0.10 — синхронизация и версия
- Версия 0.10 (beta) везде: app.js, build.gradle (versionCode 11 / versionName 0.10), strings.xml, README.
- Исправлен перенос прогресса: длинный код мог обрезаться буфером обмена/выделением на телефоне → прогресс не подтягивался.
- Добавлена надёжная резервная копия прогресса файлом: «⬇️ Сохранить копию (файл)» и «⬆️ Восстановить из файла» (sodesu-progress-YYYY-MM-DD.json).
- Импорт кода стал устойчивее: чистит пробелы/переводы строк, принимает и base64-код, и «сырой» JSON (или содержимое файла), понятные сообщения об ошибке.

## 0.10 — исправления интерфейса и сохранение файлов
- Исправлена критическая ошибка: при запуске главный экран не отрисовывался (пустые карточки/календарь). Причина — обращение к удалённым полям старой формы добавления библиотеки (`cType`/`cAdd`) в bindSettings → исключение прерывало инициализацию. Мёртвый код удалён.
- Инициализация сделана отказоустойчивой: сбой одного модуля больше не «роняет» весь интерфейс (каждый блок в защищённом вызове).
- Сохранение файлов (копия прогресса, экспорт библиотеки .csv) теперь через системный диалог с выбором места: добавлен нативный мост Android `saveFile` (Storage Access Framework) + веб-фолбэк.

## 0.10 — правки по списку
- Синхронизация: убран перенос кодом, остался только файл («Сохранить копию» / «Восстановить из файла»).
- «Полезные материалы» теперь делятся по темам (Вопросы / Формы 何 / Время), а не по урокам.
- Кнопки GitHub и Boosty: открытие во внешнем браузере (нативный мост Android `openUrl`) — больше нет «Webpage not available».
- «Зазубрить»: слово выходит из категории только после 3 отметок «Знаю» в режиме зазубривания (счётчик n/3 на кнопке); обычное «Учить»/словарь на это не влияют. В резервную копию добавлен счётчик (cramHits).

## 0.10 — сброс, огонёк, плавность
- Сброс прогресса: системный confirm в WebView не срабатывал — заменён на свой диалог «Удалить весь прогресс?» (Отмена/Удалить). После сброса обновляются все экраны.
- Кнопка-огонёк теперь открывает именно режим «Зазубрить» (только слова из этой категории). Был баг: переход на экран сбрасывал режим в обычное «Учить».
- Оптимизация словаря/грамматики: при отметке «знаю»/«★» обновляется только нужная карточка, без ререндера всего списка (≈1257 строк) — убраны лаги. Свайп переведён на transform/opacity + will-change, passive-слушатели.

## 0.10 — большой набор правок
- Окно подтверждения сброса теперь по центру экрана (добавлены стили оверлеев; раньше .onboard был без CSS).
- «Зазубрить»: после «Знаю» (если ещё не вышло из категории) и после свайпа влево (перевод) слово возвращается только через 10 минут. «Ещё» — сразу.
- Подсказки в меню (словарь, грамматика, кандзи, тест, учить, зазубрить, настройки) — показываются один раз; при новых функциях добавляются новые подсказки.
- После обновления (не первый запуск) показывается окно «Что нового» со списком изменений.
- Из словаря Genki удалены повторяющиеся слова (16 точных дублей; id остальных не меняются — прогресс сохраняется).
- Убрана «Моя библиотека» из источников.
- Экспорт библиотек: выбор, какую именно библиотеку выгрузить.
- «Восстановить из файла» и импорт .csv теперь открывают выбор файла (добавлен WebChromeClient.onShowFileChooser — в WebView это обязательно).
- При добавлении слов/кандзи/грамматики можно выбрать уже созданную тему/занятие библиотеки (или создать новую).
- В README добавлен баннер (docs/baner.png).

## 0.11.1 — CSV-библиотеки, настройка паузы, «Не знаю»
- Версия 0.11.1 (beta) везде (app.js, build.gradle vC12/vN0.11.1, strings.xml, README).
- Библиотеки по умолчанию выгружены в 3 файла .csv: libraries/Genki1.csv, libraries/Genki2.csv, libraries/Полезные материалы.csv (формат импорта приложения).
- Роадмап: «Устойчивые выражения (идиомы) в библиотеку».
- Кнопка-огонёк скрывается, если все слова «Зазубрить» на паузе (нет готовых).
- Длительность паузы «Зазубрить» вынесена в Настройки → Настройка деки (степпер −/+, 0–120 мин, по умолч. 10).
- «Зазубрить»: кнопка «↻ Ещё» заменена на «✗ Не знаю»; свайп влево после показа перевода = «Не знаю» (пауза на заданное время, счётчик «Знаю» не растёт). Свайп влево до перевода — показывает перевод.

## 0.11.1 — источники, «Дека», меню словаря
- Раздельная настройка источников: у Словаря, Кандзи и Грамматики свои наборы библиотек (Настройки → «Источники: Словарь · Кандзи · Грамматика»), независимо от деки.
- Кнопка «Выбрать всё / снять всё»: в материалах деки и в источниках каждого раздела (если всё выбрано — снимает всё).
- «Зазубрить»: по окончании (всё пройдено или на паузе) — кнопка «← На главную».
- «Дека»: если перевод открыт тапом, свайп влево отправляет слово «в деку» (оценка «Трудно», повтор позже).
- «Учить» переименован в «Дека» (меню, кнопки, подсказки).
- Кандзи: починен баг — после прокрутки тап по верхней части экрана не открывал описание (переход на click + sticky-хедер не перехватывает тапы; слушатели вешаются один раз).
- Свои библиотеки можно удалять (в редакторе библиотек — «🗑 Удалить эту библиотеку», с подтверждением).
- Словарь: долгое нажатие на слово — меню «Знаю / В деку / Зазубрить / В архив».

## 0.11.1 — общие источники, очередь деки, тап в словаре
- Источники Словаря/Кандзи/Грамматики снова общие (один набор «Библиотеки»), но отдельно от деки.
- Настройка библиотек деки переносится на несколько строк (.lib-range: flex-wrap), не обрезается.
- Свайп «назад» из «Статистики за 7 дней» возвращает на главный экран.
- «Дека»: любое действие с карточкой (Знаю/Зазубрить/Не знаю/Трудно/Легко) засчитывается как пройденная и идёт в дневной счётчик новых; исключение — «В архив».
- Исправлен баг: «на сегодня всё» при ещё оставшихся карточках — колода пересобирается из актуального состояния при опустошении сессии.
- Словарь: меню действий (Знаю/В деку/Зазубрить/Архив) открывается по одиночному тапу (не по зажатию); свайпы остаются.

## 0.11.1 — оптимизация плавности
- Списки (словарь/грамматика) больше не несут тяжёлую тень на каждой из ~1240 карточек — главный источник лагов прокрутки/отрисовки.
- content-visibility:auto для карточек словаря и плиток кандзи — браузер не отрисовывает то, что за экраном.
- Убран backdrop-filter (blur/saturate) с хедера, нижней навигации и карточек тем — дорогой репейнт при каждом кадре.
- Переключение вкладок не пересобирает список заново, если данные не менялись (ревизия данных Store.rev + maybeRender для словаря/кандзи/грамматики). Раньше каждый переход заново строил ~1240 узлов.
- Чуть короче анимация перехода между вкладками.

## 0.11.1 — оценки, темы в тестах, автообновление
- Свои библиотеки удаляются в редакторе (🗑) — подтверждено; теперь после добавления/удаления сразу обновляются и источники «Словарь/Кандзи/Грамматика» (раньше — только после «Выбрать всё»).
- В тестах раздел «Уроки / темы»: уроки и темы разных библиотек больше не путаются (составной ключ lib|l), темы выбираются как и занятия.
- Кандзи: в карточке показываются только слова, реально содержащие этот иероглиф (убрано ~967 лишних примеров).
- На карточке «Дека» при свайпе штампы: вправо — «ЗНАЮ», влево — «НЕ ЗНАЮ».
- Кнопки оценки переименованы: «Повторить завтра» (~1 день) и «Повторить через неделю» (интервал EASY теперь реально 7 дней).
- Убрана полоска-«грип» сверху всех шторок.
- Автопроверка версии: при запуске сверяется с последним релизом github.com/Severoff03/sodesu; если вышла новее — окно с кнопкой «Обновиться» (ссылка на релиз). Добавлено разрешение INTERNET.

## 0.11.2 — «Зазубрить», редактор своих библиотек, главная
- «Зазубрить»: кнопки «Не знаю»/«Знаю» поменяны местами; для выхода нужно отметить «Знаю» N раз ПОДРЯД (N настраивается: Настройки → «Знаю подряд для выхода»). «Не знаю» сбрасывает серию. Убраны счётчик «осталось» и тосты.
- Слова из «Зазубрить» больше не показываются в обычной деке.
- Дека: после «не знаю» (свайп влево) любой следующий свайп просто листает, штампы «знаю/не знаю» не показываются; интервал — по SM-2.
- Примеры к кандзи в деке — только слова, реально содержащие иероглиф (как в разделе «Кандзи»).
- Архив и «Зазубрить» теперь засчитываются в дневном счётчике новых.
- Когда все слова «Зазубрить» на паузе — экран пишет, через сколько вернуться.
- Настройка количества повторений «Знаю подряд» для выхода из «Зазубрить».
- Редактирование элементов своих библиотек: в меню словаря / шторке кандзи / шторке грамматики у своих элементов кнопка «✏️ Редактировать» — правка полей + удаление элемента.
- Главная: тап по плиткам статистики ведёт в Словарь/Кандзи/Грамматику. Кнопка «Лимиты» в деке переименована в «Настройки».
- Ежедневные уведомления (новые слова/грамматика и напоминания) отключены — воркер отменяется.
- Версия 0.11.2 (beta).

## 0.11.3 — счётчик деки, значения через «;», тесты, архив, библиотеки
- Дека: верхняя строка — «Осталось N» (новые+повторения одним числом); в «Зазубрить» строка скрыта.
- «Зазубрить»: после «Знаю» — подсказка, сколько ещё раз отметить «Знаю».
- Несколько значений у слова: разделяйте «;» — показываются списком (словарь, карточки, редактор своих слов).
- Тесты: варианты ответа берутся из того же занятия/темы и библиотеки (добор из пула, если мало) — без посторонних слов. (Часть речи не учитывается — нет данных.)
- Архивные слова в словаре — серым (приглушённые); в Настройках кнопка «Очистить архив».
- Папка libraries/ для GitHub: genki/ (готовые CSV) и jlpt/ (шаблоны N5–N1 на базе jisho) + README с инструкцией импорта.
- README и онбординг при первом запуске: как докачать библиотеки (jisho/JLPT) и ссылка на папку на GitHub.
- Версия 0.11.3 (beta).

## 0.11.3 — фикс импорта CSV
- Импорт библиотеки из .csv: убран баг с BOM (заголовок больше не попадал в данные мусором); пустой/неверный файл не создаёт пустую библиотеку; после импорта библиотека сразу появляется в источниках словаря/деки; понятные сообщения об ошибке; принимаются заголовки type/тип/word/слово и типы на русском.
- Напоминание: выбор файла в Android-WebView работает только со сборкой, где добавлен onShowFileChooser (нужна пересборка APK).

## 0.11.4 — счётчик деки, главная, редактор
- Дека: «Осталось» = карточки текущей сессии; любое взаимодействие (Знаю/Повторить завтра/через неделю/Зазубрить/Не знаю/Архив) уменьшает счётчик на 1.
- Главная: убрана строка «К повторению сегодня»; кнопка «Дека» перестаёт гореть градиентом (как обычная кнопка), когда на сегодня нет новых/повторений по словам, кандзи и грамматике.
- Редактор своих библиотек: после добавления слова/кандзи/грамматики очищаются только поля ввода — выбранные библиотека и занятие/тема остаются, можно вносить дальше.
- Версия 0.11.4 (beta).

## 0.12.4 — релизная подготовка, дека, словарь, кастомизация
- Версия вынесена в `www/js/version.js`; Android `versionCode`/`versionName` и строка версии приложения берутся из него автоматически.
- Дека: дневной остаток хранится отдельно и уменьшается при каждом действии с карточкой; изменение лимитов сохраняет уже пройденный за день объём.
- Главная: счётчик деки теперь считает очередь с учётом дневного остатка, а не только новые карточки.
- Словарь: результаты поиска подгружаются порциями по мере прокрутки, чтобы большие списки работали плавнее.
- Режим «Зазубрить» можно полностью выключить в расширенной кастомизации; связанные кнопки скрываются.
- Настройки деки, «Зазубрить» и комментарий грамматики перенесены в раскрываемый блок «Расширенная кастомизация».
- Обновлены README и release notes для публикации `Sodesu_0.12.4.apk`.
